
    <h2>Array includes()</h2>
    

        function arrayIncludes(array, value){
            return array.includes(value);
        }
        const fruits = ['Apple', 'Banana', 'Cherry'];
        const searchValue = 'Banana';

        const result = arrayIncludes(fruits,searchValue);
        console.log(result);
        
    
